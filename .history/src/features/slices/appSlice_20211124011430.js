import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const appSlice = createSlice({
  name: sliceName,
  initialState,
  reducers: {},
});

export const {} = appSlice.actions;
export const selectApp = state => state.app.va
export default appSlice.reducer;
